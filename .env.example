PORT=3000

# SMTP (opzionale, se non presente l'app salva l'invio ma non manda email)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=utente@example.com
SMTP_PASS=password
SMTP_FROM=moduli@example.com
